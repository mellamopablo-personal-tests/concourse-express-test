platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: 8

inputs:
- name: app

outputs:
- name: modules
  path: node_modules

run:
  path: ./app/scripts/dependencies.sh
